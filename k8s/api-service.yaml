apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: sample-atg
  labels:
    app.kubernetes.io/name: sample-atg
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sample-atg
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
