apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-data
  namespace: sample-atg
  labels:
    app.kubernetes.io/name: sample-atg
    app.kubernetes.io/component: mongo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
